

export const CTAButton = ({ children }) => {
  return (
    <button className="bg-gradient-to-r from-[#4551d0] to-[#3c9fd3] text-white text-sm font-medium px-5 py-3 rounded-full shadow-lg hover:opacity-90 transition">
      {children}
    </button>
  )
}
